<%= render 'show', runner: @runner %>

<p class="lead">
        <% if user_signed_in? %>
        <%= link_to 'Edit', edit_runner_path(@runner), class: "btn btn-success btn-sm" %>
        <% end %>
        <%= link_to 'Back', runners_path, class: "btn btn-danger btn-sm" %>
    </p>
  </div>
